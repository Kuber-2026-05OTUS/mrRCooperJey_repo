apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homework-pvc
  namespace: homework
spec:
  storageClassName: "homework-storage-class"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
